doctype html
html(lang='en')
  include common/head.pug
  +head("Starter MVP")
  body
    header
      include common/navbar.pug
      +navbar()
      .container
        .row.align-items-center
          .col-md-6.order-md-1.text-center.text-md-left.pr-md-5
            h1.mb-3 Basic CRUD Application using NodeJs
            p.lead This app explores how to build a NodeJS web application covering key topics.
            form(action="/", method="POST")
              .input-group.mb-3
                input(name="lead_email", type="email", placeholder="Enter your email address").form-control
                .input-group-append
                  button(type="submit").btn.btn-primary Get Started
    if leads
      h1 Leads
      for lead in leads
        p(id=lead.id)
          span #{lead.email}
          |
          a(href="/lead/"+lead.id) Details
          |
          button(onClick=`deleteLead('${lead.id}')`) Delete
    include common/footer.pug
    +footer()
    script(src='/javascripts/leads.js')
    
